// Librerías
const express = require('express');
const app = express();

// Puertos y objetos
const FRONTPORT = 9998;
const BACKPORT = 9999;

// Controlador con REST API

const 

// 
app.listen(FRONTPORT, () => {
	console.log("Server en puerto " + FRONTPORT);
});


